server:
  port: 8080
spring:
  application:
    name: address
  datasource:
    url: jdbc:hsqldb:mem
    password: mines
    username: mines
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
# Uncomment to automatically generate a database
#    generate-ddl: true
#    hibernate:
#      ddl-auto: update


logging:
  level:
    org:
     springframework:
       web: DEBUG